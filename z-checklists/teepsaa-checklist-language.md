# Teepsaa — Khmer / English Language Toggle

## How it works

A toggle button in the header sets a session variable (`$_SESSION['lang']`). Every page loads a language file based on that variable. All UI text is pulled from the language file rather than hardcoded.

---

## Done

- [x] Logo swap — header shows `teepsaa_logo_khm.png` when `lang=km`, `teepsaa_logo_eng.png` when `lang=en`
- [x] Create `lang/en.php` — lookup table of keyed strings (100+ keys covering buyer/vendor/public surfaces: homepage, search, product, cart, checkout, login, register, orders, wishlist, settings, vendor dashboard, messages, about, footer)
- [x] Create `lang/km.php` — matching Khmer translations for every key (real Khmer, not machine-translated placeholders)
- [x] Add language loader to header (`header/header.php`) — loads `$t` from the right file based on `$_SESSION['lang']`, defaults to `km`
- [x] Toggle button already existed in header (flag dropdown, `lang/set.php`) — sets session lang and reloads
- [x] **Header wired** (2026-06-19) — every hardcoded string in `header/header.php` now reads from `$t[...]`: search placeholder, Login, admin nav (Admin/Orders/Marketing/Messages), buyer/vendor/admin dropdown menus (Orders/Products/Messages/Vendor/Settings/Logout/Wishlist/Cart), notifications dropdown (Notifications/Mark all read), Language/Currency labels, and the mobile-nav duplicate of all of the above. Added 6 new keys to both lang files to support this: `nav_admin`, `nav_marketing`, `nav_notifications`, `nav_mark_all_read`, `lang_label`, `currency_label`.
- [x] **Footer wired** (2026-06-30) — all footer labels now read from `$t[...]`: the three column headings (`footer_your_account`, `footer_help`, `footer_brand`), the role-aware Your Account links, the Help column (Help Center/Shipping/Returns/Privacy/Terms), the teepsaa column (About/Careers/Sell on teepsaa), and the copyright. Added `$t` guard-load to `footer/footer.php` so it works even without the header. New keys: `footer_sign_in`, `footer_create_account`, `footer_brand`. Renamed `footer_shipping` → "Shipping" / "ការដឹកជញ្ជូន". Khmer corrections: returns = `ការប្រគល់ទំនិញវិញ` (added វិញ = "back").
  - **Brand decision:** the Khmer brand renders uniformly as **`ទីផ្សារ`** (the marketplace pun) everywhere — also updated `search_placeholder` and `about_title`. No more phonetic `តិបសា`.
  - **Tagline** is per-language: EN "Shopping made easy" (Pacifico, `--font-script`), KM "ទិញឱ្យងាយស្រួល" (Metal, `--font-script-km`), targeted via `.footer-tagline:lang(km)`.

---

## Build Steps (remaining — one page at a time, user reviews each before moving on)

- [x] Footer (`footer/footer.php`) — done 2026-06-30 (see Done section)
- [x] **Khmer web font** (2026-06-30) — Noto Sans Khmer loaded in `style.css` `@import` and added to the body font stack (`"Source Sans 3", "Noto Sans Khmer", …`). Per-glyph fallback means Latin uses Source Sans 3, Khmer uses Noto Sans Khmer automatically app-wide — no `:lang()` needed.
- [x] Homepage (`index.php`) — section headings wired via `home_*` keys, 2026-06-30. NOTE: `product_card()` (defined in `index.php`) is **data-only** (name/price/seller/rating) — no translatable text, so nothing to wire there.
- [x] **Bilingual banners** (2026-06-30) — `banners` table gained `title_km` / `subtitle_km` (`database/migration-banner-lang.sql`). Admin (`admin/banners.php`) now supports **editing** existing banners (was upload/delete only) with separate EN + KM title/subtitle fields. The carousel (`includes/banner-carousel.php`) shows Khmer copy when `lang=km`, falling back to English when a KM field is blank. Marketing can write language-specific banner copy, not just translations.
- [x] **Search page** (`search/index.php`) — done 2026-06-30. Wired all filter UI (Filters, Sort by, Category, All categories, Price/Min/Max, Apply, Rating, "★ N & up", Clear filters), the result count ("N products for …"), no-results + filter hint, and the **sort labels** (Newest / Price asc / Price desc / Top rated / Most popular) + filter chips. Loaded `$t` early (sort labels are built before the header). New keys added: `search_no_results_hint`, `search_price_min/max`, `search_rating_up`, `search_products`, `search_for`, `search_from`, `sort_newest/price_asc/price_desc/rating/popular`. Product cards = data-only, nothing to translate.
- [x] **Product page** (`product/index.php`) — done 2026-07-01. Wired markup (Sold by, Variants, In/Out of stock, Add to cart, Login to buy, Save/Saved, Added-to-cart flash, Reviews, No reviews) AND the inline `<script>` strings (Select options, Combination not available, stock states, "Please select …", out-of-stock hints) by echoing `$t` into the JS. New keys: `product_in_stock`, `product_select_options/variant`, `product_combo_unavailable`, `product_option_oos`, `product_variant_oos`, `product_save/saved`, `product_please_select`, `product_and`, `product_please_select_variant`.
- [x] **Cart / checkout** (`cart/index.php`, `checkout/index.php`) — done 2026-07-01. Cart: title, empty state, each/Update/Remove, subtotal, all delivery states (Grab delivery / set address / set pin / out-of-range / est. + COD), total, checkout btn. Checkout: order-placed, address switcher (Delivering to / Change / Use this address / Default), order summary, delivery lines, totals, COD notes, ABA scan instructions (sprintf w/ amount), notes placeholder, "I've paid", confirm note, and the Grab-delivery modal. ~15 new `cart_*`/`checkout_*` keys (long sentences w/ embedded `<strong>` kept in the value; `checkout_scan_instructions` is a sprintf template). Lint clean, key parity + sprintf verified. NOTE: not visually rendered — both pages require a logged-in buyer with cart items; verify by logging in as a buyer and toggling language.
- [x] **Login / register** (buyer + vendor) — done 2026-07-01. Wired all 6 pages: `login/`, `login-buyer/`, `login-vendor/`, `register/`, `register-buyer/`, `register-vendor/` (incl. the vendor "separate account" explainer). New keys: `login_no_account`, `login_vendor_title`, `register_have_account`, `register_as_vendor`, `register_promo`, `form_optional`, `auth_agree` (sprintf template with Terms/Privacy links), `sell_cta_*`. Titles (`<title>`) left English (head renders before `$t` loads).
- [x] **Buyer dashboard** (2026-07-01 → 2026-07-02) — DONE: wishlist, orders list, settings (all tabs + address JS), messages (`messages-buyer/index.php` + `thread.php`), the **shared** `order-status.php`/`refund-status.php` status bars (with `$t` guard), AND the **order detail** page (`dashboard-buyer/order.php`) with the full **refund/return flow** (badge label map, order info/items, refund-request form, return-dispatch). `$t` loaded early in order.php (status badge built pre-header). Refund reason `<option>`s got explicit English `value=` so stored values stay English while the label translates. The three refund/return **action** files (`refund-request.php`, `confirm-delivery.php`, `return-dispatch.php`) are POST handlers with NO UI — nothing to translate. EN/KM parity 334/334. `orders/index.php` = unlinked legacy view (header/footer point Orders → `/dashboard-buyer/`); skipped.
- [x] **Vendor dashboard** (2026-07-02) — DONE: main dashboard (`dashboard-vendor/index.php` — business status badge, trial banner w/ sprintf, analytics stats + best sellers, orders/products sections, stock/status badges; `$t` loaded early for the pre-header status label) and **messages-vendor** (`index.php` + `thread.php`, reuses buyer message keys + new `messages_closed_notice`/`messages_contact_lower`). Parity 354/354. DONE ALSO: **orders-vendor** (`index.php` tabs/lists, `order.php` detail w/ payout breakdown + dispatch COD warning, `refund.php` refund handling + return-received) — `$t` loaded early for status badges; reuses `order_badge_*`/`order_col_*`. Parity 382/382. DONE ALSO: vendor **settings** (`dashboard-vendor/settings/` — all 5 tabs account/address/business/bank-QR/password/danger + address JS; reuses buyer `settings_*` + ~20 new vendor keys) and **submit** (`submit/index.php` — business submission form). Parity 413/413. DONE ALSO: **`products/index.php`** (2026-07-02) — the 1156-line product manager: subnav, edit-preview panel (status controls, cancel-sale), add/edit form (all fields + sale scheduling + variants/option-types + photos), the archive tab, the product list table, AND the inline JS strings (category cascade, payout preview, variant/combo builder placeholders — echoed `$t` into the JS). ~46 new `prod_*` keys. **VENDOR DASHBOARD COMPLETE.** Parity 459/459. Reminder: admin stays English; vendor bilingual *content* inputs are a separate parked batch.
- [~] About / static pages — **About page DONE** (2026-07-02, `about/index.php` fully wired: lead + Our mission / For buyers / For vendors / Built for Cambodia sections + CTAs; 13 new `about_*` keys, reuses `about_title`/`footer_sell_on`). Parity 470/470. **Privacy DONE** (2026-07-02) — `privacy/index.php`, full KM translation via a per-page `<?php if ($lang === 'km') ?>` bilingual body (both versions inline; NOT $t keys — keeps shared lang files clean and gives the Khmer legal reviewer one block per page to edit). All 9 sections. **Terms DONE** (2026-07-02) — `terms/index.php`, all 14 sections via the same `$lang` bilingual body. **Shipping DONE** (2026-07-02) — 4 sections. **Returns DONE** (2026-07-02) — 3 sections. **Help/FAQ DONE** (2026-07-02) — `help/index.php`, `$faqs` array built bilingually (set `$lang` early, if/else), all 6 sections + 23 Q&A pairs; hero/CTA via `$t`+inline. **ALL CONTENT PAGES COMPLETE** (privacy, terms, shipping, returns, help, about). Future features planned in separate docs: `teepsaa-checklist-content-cms.md` (admin-editable pages) + `teepsaa-checklist-admin-rbac.md` (tiered admin access). (Legal pages will get native Khmer review later per user.)
- [x] Test both languages across all wired pages — ongoing throughout; each page verified rendering in KM + EN with lint + no PHP errors as it was wired. Public pages spot-checked end-to-end; auth-gated pages verified via lint + key parity + clean 302s (need a logged-in session to eyeball visually).

---

## ✅ CORE UI PASS COMPLETE (2026-07-02)

The translatable UI of the main shopping/selling flows is bilingual EN/KM: header, footer, Khmer font (Noto Sans Khmer), homepage, bilingual banners, search, product, login/register (all portals), cart/checkout, buyer dashboard (incl. order detail + refund flow), vendor dashboard (dashboard/orders/settings/messages/submit/products), and all content pages (about/privacy/terms/shipping/returns/help). Admin stays English by design. ~470 `$t` keys, EN/KM parity maintained.

---

## 🔲 REMAINING $lang TOGGLE WORK (audited 2026-07-02)

Everything below still does NOT respond to the language toggle. Grouped by type.

### A. UI pages — ✅ COMPLETE (2026-07-02)
- [x] `contact/`, `contact-buyer/`, `contact-vendor/` — contact forms **(DONE)** — issue-type `<option>`s use value→label maps so stored values stay English; reused `messages_contact`/`form_optional`/`btn_cancel`; `contact_signin_note` sprintf with 2 links.
- [x] `business/` — public vendor storefront **(DONE)** — rating review singular/plural, Products heading, in-stock/out-of-stock.
- [x] `review/` — leave-a-review form **(DONE)** — back link, title, star hint, comment placeholder, submit, + JS star labels (Terrible…Excellent) echoed via `$t`.
- **GROUP A DONE** — all 11 pages (6 auth-recovery + 3 contact + business + review). ~50 new keys total. Parity 529/529.
- [x] `forgot-password-buyer/`, `forgot-password-vendor/` — password recovery **(DONE 2026-07-02)**
- [x] `reset-password-buyer/`, `reset-password-vendor/` — password reset **(DONE 2026-07-02)**
- [x] `verify-email/`, `resend-verification/` — email verification **(DONE 2026-07-02)**
- **Auth-recovery batch DONE** — 6 pages wired; new keys `auth_back_login`, `fp_*`, `rp_title`, `ve_*`, `rv_*` (email templates use sprintf `%s`); reused `login_email`/`settings_new_pw`/`settings_confirm_pw`. Parity 487/487. verify-email + resend-verification are session-gated (verified via lint + clean 302).
- STILL TODO in Group A: `contact/`, `contact-buyer/`, `contact-vendor/` (contact forms), `business/` (public storefront), `review/` (review form).

### B. User/vendor/admin-entered content — doesn't toggle (stored in one language)
This is the **parked "vendor bilingual content inputs" feature**, but it's broader than products. Needs schema (EN + KM columns, display picks by `$_SESSION['lang']` with EN fallback — same as bilingual banners) + admin/vendor form changes. NOT just `$t` wiring.
- [x] **Category names** ✅ DONE (2026-07-02) — `categories.name_km` column (`database/migration-category-lang.sql`, applied). Admin editor (`admin/categories.php` + `category-action.php`) has EN + KM name inputs on add/edit. Shared helper `cat_name($row)` in `config/db.php` (picks `name_km` when `lang=km`, else `name`). Wired display everywhere: homepage tiles, search filter + active-filter chip, vendor product-manager cascade (JS via `CAT_LANG` + `name_km`) + edit preview, submit cascade (JS), vendor business-settings checkboxes (display KM, keep EN `value=` since `businesses.category` stores names). Verified: KM shows Khmer name, EN falls back. NOTE: homepage tile `href` still searches by English `name` (q param) — intentional, keeps search behavior stable. `businesses.category` still stores English category names (the checkbox values) — fine.
- [x] Product name & description (vendor) ✅ DONE (2026-07-02) — `products.name_km` + `products.description_km` (`database/migration-product-lang.sql`, applied). Generic helper `lang_field($row, $field)` added to `config/db.php` (`cat_name` now delegates to it). Vendor form (`products/index.php`) has KM name + KM description inputs (`form_km_field` label key); `products/save.php` saves both on add + edit. Display wired on ALL live sites: product page (h1 + description), homepage `product_card` (all 7 feed queries fetch `name_km`), search cards (both server blocks) + `/api/search/` load-more (resolves name/description server-side, unsets `_km` before json_encode), business storefront cards, wishlist, cart + checkout (live joins → `product_name_km` alias + inline pick), product-manager list + edit preview. Verified KM shows Khmer, EN falls back. **DEFERRED:** `order_items.product_name` is a purchase-time SNAPSHOT — order-detail pages (`dashboard-buyer/order.php`, `orders-vendor/order.php`) show the name as snapshotted; bilingual there needs an `order_items.product_name_km` column + checkout-confirm change (money path) — later. Product page `<title>` + SEO meta intentionally stay English.
- [x] Business/store name & description (vendor) ✅ DONE (2026-07-02) — `businesses.name_km` + `businesses.description_km` (`database/migration-business-lang.sql`, applied). New helper `pick_lang($base, $km)` in `config/db.php` for **aliased** columns (e.g. joined `business_name`/`business_name_km`) — complements `lang_field()` for `field`/`field_km` rows. Vendor settings form (`dashboard-vendor/settings/index.php`) has KM store-name + KM description inputs; `business-action.php` saves both; the `$business` SELECT now fetches the KM columns for prefill. Display wired: storefront (`business/index.php`) name h1 + description; buyer-facing **seller name** on homepage cards (7 feeds + `product_card`), search cards (both server blocks) + `/api/search/` + `/api/recently-viewed/` (added missing `session_start()` there so lang resolves), product page "sold by", wishlist, cart + checkout seller group headers. Verified KM shows Khmer, EN falls back. **NOT localized (intentional):** vendor-facing dashboards/orders show the vendor's OWN store name (pointless), order/confirm snapshots, storefront `<title>`/SEO (English convention).
- [x] Variant / option-type labels (Size, Color, S/M/L) ✅ DONE (2026-07-02) — `product_option_types.name_km`, `product_option_values.label_km`, `product_variants.label_km` (`database/migration-variant-lang.sql`, applied). Vendor builder in `products/index.php` — BOTH modes get a KM box beside each EN box: option-type builder (name + each value), and the legacy simple variant list (each variant row); tinted `#fafafa` so vendors can tell them apart. `products/save.php` stores all KM fields; the composed variant `label_km` is auto-built from the value KM labels (falls back to the EN value per-part, stored NULL if identical to EN). Display wired: product page (option-type name, value label, single-variant label — all `lang_field`), cart + checkout variant label (`variant_label_km` alias + `pick_lang`). Verified KM shows Khmer (Small→តូច, Size→ទំហំ), EN falls back. Save path runs through the JS builder (parallel inputs, additive) — lints clean, page loads. **DEFERRED:** `order_items.variant_label` snapshot (order-detail + review pages) — same purchase-time-snapshot limitation as product_name.
- [x] Job postings / careers (admin-entered) ✅ DONE (2026-07-02) — `job_postings.title_km` + `location_km` + `description_km` (`database/migration-jobs-lang.sql`, applied). `employment_type` is a fixed select → translated via a `$t` map (`emp_full_time`…`emp_freelance`), stored value stays English. Admin form (`admin/careers.php`) has KM inputs for title/location/description (English chrome per admin rule); `careers-action.php` saves them. Public `careers/index.php` + `apply.php` fully localized: chrome wired to new `careers_*`/`apply_*` keys AND job content via `lang_field` + the emp-type map (reused `register_name`/`login_email`/`form_optional`). Verified KM shows Khmer, EN + no-KM-fields fall back. Admin notification email + `<title>` stay English (admin-facing / head convention).

**✅ GROUP B COMPLETE** (2026-07-02) — all user/vendor/admin content is bilingual: category names, product name/description, business name/description, variant/option labels, job postings. Two shared helpers in `config/db.php`: `lang_field($row,$field)` (for `field`/`field_km` rows) and `pick_lang($base,$km)` (for aliased columns). Pattern everywhere: KM optional, falls back to EN.
- [ ] (Lower priority / may leave as-is: reviews, support messages, refund reason free-text, buyer addresses — user-authored, not really "translatable")
- DONE already (reference pattern): **banners** (`title`/`title_km`, `subtitle`/`subtitle_km`).

### C. Emails & notifications — ✅ DONE (2026-07-04)
- [x] Notification **emails** — **bilingual: Khmer on top, English below** (avoids needing to know the recipient's language). New renderer `notification_email_html_bi(headingKm,bodyKm,headingEn,bodyEn,ctaKm,ctaEn,url)` + `email_subject_bi(km,en)` in `config/notify.php` (KM block · divider · EN block · one bilingual CTA button "KM · EN" · bilingual footer). Converted every user-facing email: order received (checkout/confirm — shared item table with bilingual labels), payment confirmed, order dispatched, payout sent, delivery confirmed, low stock, abandoned cart, review reminder (crons), email verification code (register-buyer/-vendor + resend), password reset (forgot buyer/vendor). **Only English left:** the job-application email → admin (internal/staff-facing).
- [x] **In-app notifications** (bell) — **render in the current toggle language** at display time. Added `notifications.data` JSON column (`migration-notification-data.sql`); `notify()` takes optional `$data` (order ref, product name, units); all 7 call sites pass it (English `message` kept as fallback). `notification_text($row,$t)` translator in `config/notify.php` maps `type`→`notif_*` key + sprintf(params); `api/notifications/` loads the session-lang dictionary and translates message + time-ago (`notif_just_now`/`min`/`hour`/`day_ago`). Old rows with no `data` fall back to the stored English. Verified via unit test (KM/EN/low-stock/fallback) + parity 563/563.

### D. Global JS strings — ✅ DONE (2026-07-04)
- [x] `header/header.php` emits `window.T = {…}` (json_encode of the `js_*` keys in the current language, `JSON_HEX_TAG` etc. for safety). `js/status-refresh.js` reads `window.T` for the status-bar labels (pending…refunded), "Order cancelled"/"Refund rejected", and toasts (session-expired, order-updated, refresh error). `js/notifications.js` reads it for "No notifications yet" / "Loading…". `js/popup.js`, `js/map.js`, `js/boundary.js` have no user-facing text. All with English fallbacks (`|| '…'`). ~23 `js_*` keys added, parity kept.

### 📧 Email-template EDITOR (staff-editable emails) — ✅ DONE (2026-07-04)
Emails moved from hardcoded PHP into a DB table so admin staff can edit the wording.
- `email_templates` table (`migration-email-templates.sql`) + `config/email-templates.php` defaults (source of truth **and** safe fallback) + `database/seed-email-templates.php`. 10 templates seeded.
- `render_email_template($pdo, $key, $data)` in `config/notify.php`: loads the editable row (falls back to defaults so a bad lookup never breaks a send), substitutes `{tokens}`, returns `[subject, html]` via the bilingual renderer. **All 12 call sites refactored** to use it (also fixed: register/forgot files now require `notify.php`, not just `mail.php`).
- Admin UI under `admin/messages/`: `emails.php` (lists all 10 types → click a row) → `email-edit.php?key=…` (bilingual subject/heading/body/CTA fields + placeholder hints + **live iframe preview** with sample values) → `email-save.php` (validates + **blocks removing a required `{token}`**). Linked from the support-messages page header ("✉️ Email templates").
- Verified: DB-render matches, a staff body edit goes live in the email immediately, token-deletion is blocked, pages 302 for non-admins, all lint clean.

### E. Infrastructure / minor
- [x] **Lang persistence** ✅ DONE (2026-07-04) — `buyers`/`vendors` already had a `lang` column (was unused). `lang/set.php` now writes the choice to that column for logged-in buyers/vendors; buyer + vendor login (`login-buyer.php`/`login-vendor.php`) load it into `$_SESSION['lang']`. Column default aligned to `km` (`migration-lang-default-km.sql`) to match the app default. So the preference follows the user across devices/sessions.
- [x] Date formatting ✅ DONE (2026-07-04) — `config/i18n.php` adds `fmt_date($fmt, $when)` (mirrors `date()` arg order) + `km_num()`; required globally via `config/db.php`. In Khmer it translates month + weekday names, am/pm, and digits to Khmer numerals; in English it's identical to `date()`. Swept **12 non-admin display files** (`date('M …'` / `d M` / `D` / `F` / `g:i` / `H:i` → `fmt_date(`) — dashboards, order pages, message timestamps, product page, products list. Left `date('Y-m-d…')` DATA/input formats untouched, and left admin/cron English. NOTE: Khmer date keeps the English field order (Month day, Year → "កក្កដា ៤, ២០២៦"); idiomatic day-month reordering was not done (minor, understandable as-is).
- [ ] `<title>` tags — **intentionally left English** (brand convention). Rationale: browser-tab text only; the visible page content is already Khmer; localizing needs an early `$t` load before `<head>` on ~50 pages for near-zero user benefit. Revisit only if desired.
- Admin (`/admin/*`) — intentionally English, NOT on this list.

### Suggested order
1. **11 UI pages** (A) — fast, closes the visible gaps.
2. **Category names** (B) — highest-visibility content item.
3. **Lang persistence** (E) — cheap, and unblocks translated emails.
4. Product/business/variant bilingual inputs (B) — the big feature.
5. Emails/notifications (C), JS strings (D), titles/dates (E) — polish.

**Separate planned features (own docs):**
- Admin-editable content pages (CMS) — `teepsaa-checklist-content-cms.md`.
- Tiered admin access (RBAC) — `teepsaa-checklist-admin-rbac.md`.

---

## How to resume in a new session

Tell Claude: *"Continue the Khmer/English language wiring — header is done, do the footer next."* Point it at this checklist file and `lang/en.php` / `lang/km.php` for the existing keys. Approach so far: wire one page/section at a time, swap hardcoded text for `$t['key']`, no structural/CSS/JS changes, user reviews each page before moving to the next.

---

## Notes

- Most remaining work is just swapping hardcoded text for `$t['key']` — the string keys for many pages already exist in `lang/en.php` and `lang/km.php` from earlier work, so check there before adding new keys
- Khmer font rendering may need a specific web font — **Noto Sans Khmer** (Google Fonts, free) is the standard choice
- User language preference could optionally be saved to the `users` table so it persists across sessions instead of just `$_SESSION['lang']`
- **Scope decision (2026-07-01):** the **admin dashboard stays English-only** — do NOT wire `$t` into `/admin/*`. The **vendor dashboard DOES get Khmer** (chrome/UI wiring like the buyer side).
- **Future batch — vendor bilingual content inputs:** vendor-authored content (product name/description, etc.) should eventually support separate EN + KM inputs, same pattern as the bilingual banners (`title` + `title_km`, display picks by `$_SESSION['lang']` with English fallback). This is a data-model + admin-form change, not just `$t` wiring — plan it as its own batch after the dashboard chrome is done.
